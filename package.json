{
  "name": "3d-model-viewer",
  "version": "1.1.0",
  "private": true,
  "description": "Build pipeline for the 3D Model Viewer WordPress plugin.",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "build": "vite build",
    "stage-plugin": "./scripts/stage-plugin.sh",
    "package-plugin": "rm -rf dist && ./scripts/stage-plugin.sh && (cd dist && zip -qr 3d-model-viewer.zip 3d-model-viewer)"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  },
  "dependencies": {
    "three": "^0.160.1"
  }
}
